{
  "name": "firefox-markdown",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "dependencies": {
    "marked": "^15.0.0",
    "shiki": "^1.24.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
